name: Healthcheck CI

on:
  push:
    branches:
      - main
    paths:
      - "tools/healthcheck/**"

  pull_request:
    branches:
      - "**"
    paths:
      - "tools/healthcheck/**"

jobs:
  coverage:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tools/healthcheck

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: "3.11"

      - name: Install dependencies
        run: uv sync

      - name: Check formatting
        run: uv run ruff format --check .

      - name: Check linting
        run: uv run ruff check .

      - name: Run tests & generate HTML coverage report
        # TODO: try to mock the env variables in the tests directly
        env:
          RABBITMQ_URL: "foo"
          RABBITMQ_MONITORING_USERNAME: "foo"
          RABBITMQ_MONITORING_PASSWORD: "bar"
          DISPATCHER_INSTANCES: "bar"
        run: |
          uv run coverage run -m unittest tests/*.py
          uv run coverage report -m
          uv run coverage html

      - name: Upload HTML coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          # Working directory is not used in the action
          # https://github.com/actions/upload-artifact/issues/232
          path: tools/healthcheck/htmlcov
          retention-days: 7

  security-scan:
    runs-on: ubuntu-latest
    name: Security Scanning
    defaults:
      run:
        working-directory: tools/healthcheck

    steps:
      # 1️⃣ Checkout repository
      - uses: actions/checkout@v5

      # 2️⃣ Install uv (Python 3.11 included)
      - name: Set up uv with Python 3.11
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
          python-version: "3.11"

      # 3️⃣ Install security tools via uv
      - name: Install security tools
        run: |
          uv add --dev uv-secure semgrep

      # 4️⃣ Dependency scan
      - name: Scan dependencies (uv-secure)
        run: |
          uv run uv-secure uv.lock --format json > uv-secure.json || true

      # 5️⃣ Semgrep scan (code scanning)
      - name: Run Semgrep
        run: |
          # Exclude virtual environments, cache, and non-source directories
          uv run semgrep --config auto --sarif --output semgrep.sarif \
            --exclude='.venv' \
            --exclude='.cache' \
            --exclude='__pycache__' \
            --exclude='.git' \
            --exclude='.pytest_cache' \
            --exclude='node_modules' \
            --exclude='build' \
            --exclude='dist' \
            --exclude='.tox' \
            --exclude='.mypy_cache' \
            --exclude='uv.lock' \
            --exclude='*.pyc'

      - name: Fix Semgrep SARIF file paths for GitHub Security tab
        if: always() && hashFiles('tools/healthcheck/semgrep.sarif') != ''
        uses: ./.github/actions/fix-trivy-sarif
        with:
          sarif-file: 'tools/healthcheck/semgrep.sarif'
          scan-path: 'tools/healthcheck'

      - name: Upload Semgrep results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: tools/healthcheck/semgrep.sarif
          category: semgrep-healthcheck

      # 6️⃣ Trivy scan (filesystem, Dockerfile, configs, secrets)
      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@0.33.1
        with:
          scan-type: fs
          scan-ref: tools/healthcheck
          scanners: vuln,secret,config
          format: sarif
          output: tools/healthcheck/trivy-filesystem.sarif
          ignore-unfixed: true
          exit-code: '0'

      - name: Run Trivy Dockerfile scan
        uses: aquasecurity/trivy-action@0.33.1
        with:
          scan-type: config
          scan-ref: tools/healthcheck/Dockerfile
          scanners: vuln,config
          format: sarif
          output: tools/healthcheck/trivy-dockerfile.sarif
          ignore-unfixed: true
          exit-code: '0'

      - name: Fix Trivy filesystem SARIF file paths for GitHub Security tab
        if: always() && hashFiles('tools/healthcheck/trivy-filesystem.sarif') != ''
        uses: ./.github/actions/fix-trivy-sarif
        with:
          sarif-file: 'tools/healthcheck/trivy-filesystem.sarif'
          scan-path: 'tools/healthcheck'

      - name: Upload Trivy filesystem results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: tools/healthcheck/trivy-filesystem.sarif
          category: trivy-healthcheck-filesystem

      - name: Fix Trivy Dockerfile SARIF file paths for GitHub Security tab
        if: always() && hashFiles('tools/healthcheck/trivy-dockerfile.sarif') != ''
        uses: ./.github/actions/fix-trivy-sarif
        with:
          sarif-file: 'tools/healthcheck/trivy-dockerfile.sarif'
          scan-path: 'tools/healthcheck'

      - name: Upload Trivy Dockerfile results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: tools/healthcheck/trivy-dockerfile.sarif
          category: trivy-healthcheck-dockerfile

      # 7️⃣ Upload all security scan reports as a single artifact
      - name: Upload Security scan reports as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-reports-healthcheck
          path: |
            tools/healthcheck/semgrep.sarif
            tools/healthcheck/trivy-filesystem.sarif
            tools/healthcheck/trivy-dockerfile.sarif
            tools/healthcheck/uv-secure.json
          retention-days: 7
          if-no-files-found: warn
