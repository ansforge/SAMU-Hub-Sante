name: LRM Client CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'web/lrm/client/**'
  pull_request:
    branches:
      - '**'
    paths:
      - 'web/lrm/client/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web/lrm/client

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          # Using complete path as working-directory is not working with 'use' steps
          # https://github.com/actions/setup-node/issues/706
          node-version-file: 'web/lrm/client/.nvmrc'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint:js

      - name: Run Stylelint
        run: npm run lint:css

      - name: Run Prettier check
        run: npx prettier --check "**/*.{js,ts,vue,css,scss}"

  security-scan:
    uses: ./.github/workflows/security-scan.yml
    with:
      working-directory: 'web/lrm/client'
      project-type: 'nodejs'
      node-version: '24.11'
      component-name: 'lrm-client'
      scan-dockerfile: true
      semgrep-excludes: '--exclude=".nuxt"'